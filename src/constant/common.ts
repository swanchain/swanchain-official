export enum EPos {
  LEFT = 'left',
  RIGHT = 'right'
}

export enum EPosition {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center'
}

export enum EWindowSize {
  XS = 'xs',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl'
}
